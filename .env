# Call the API directly in dev (avoids Vite proxy 502 when the proxy target is wrong).
# Must match backend `PORT`. Default in server.js / backend/.env.example is 4000.
VITE_API_URL=http://localhost:4001
